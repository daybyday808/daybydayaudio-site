# Day by Day Audio — Website (Next.js + Tailwind)

## Quick start
1. Install deps
   ```bash
   npm install
   ```
2. Run dev server
   ```bash
   npm run dev
   ```
3. Open http://localhost:3000

## Notes
- UI components (Button, Card) are implemented locally in `components/ui`.
- Replace placeholder links and images as you go.